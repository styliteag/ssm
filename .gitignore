.direnv
.env
.env.*
!.env.example
!.env.*.example
logs/

# Databases (the root ssm.db legacy snapshot is deliberately untracked)
*.db
*.bak
cookies.txt

# SSH keys mounted into the dev container — never commit.
phoenix/keys/

# Docker prod data (real DB, htpasswd, keys)
docker/data/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp

.claude/scheduled_tasks.lock
.claude/settings.local.json
.htpasswd

# Added by code-review-graph
.code-review-graph/

# Private per-machine agent instructions
CLAUDE.local.md

# Checkout-local just recipes (justfile `import?` hook)
local.just
